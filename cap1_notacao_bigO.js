//O() -> tempo de execução

// n representa o número de itens que estão sendo processados, ou seja, 
// o tamanho do array

//Ele analisa o algoritmo na pior das hipóteses (analisa CADA item do array), 
//existe o caso médio também, mas será falado só
//no cap.4

// O(log n): pesquisa binária; 
// O(n):pesquisa simples; 
// O(n * log n):quicksort; 
// O(n^2):ordenação simples; 
// O(n!): caxeiro viajante

//a notação não é medida por segundos, mas pelo crescimento de OPERAÇÕES