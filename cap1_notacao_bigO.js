//O() -> tempo de execução

//Ele analisa o algoritmo na pior das hipóteses (analisa CADA item do array), existe o caso médio também, mas será falado só
//no cap.4

//O(log n): pesquisa binária; O(n):pesquisa simples; O(n * log n):quicksort; O(n^2):ordenação simples; O(n!)

//a notação não é medida por segundos, mas pelo crescimento de OPERAÇÕES